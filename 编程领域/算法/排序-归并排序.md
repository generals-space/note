```py
import math

arr = [5, 38, 15, 48, 44, 3, 36, 26, 50, 27, 2, 46, 4, 19, 47]

def merge_sort(arr):
    length = len(arr)
    ## 结束条件
    if length < 2: return arr

    middle = math.floor(length/2)
    left = arr[0:middle]
    right = arr[middle:length]
    ## 注意此时传入的不是数组的索引, 而是子数组, 因为归并排序是out-place形式.
    return merge(merge_sort(left), merge_sort(right))

def merge(left, right):
    '''
    left与right都是有序数组, 所以情况会简单很多
    '''
    result = []

    while len(left) > 0 and len(right) > 0:
        if left[0] < right[0]:
            result.append(left[0])
            left = left[1:]
        else:
            result.append(right[0])
            right = right[1:]
    ## 经过上面的while循环, left/right中只有一个数组中还有成员, 且可能不只一个
    ## 如下两句将其追加到result列表中.
    while len(left) > 0: 
        result.append(left[0])
        left = left[1:]
    while len(right) > 0: 
        result.append(right[0])
        right = right[1:]

    return result

print(arr)
arr = merge_sort(arr)
print(arr)

```

```js
var arr = [5, 38, 15, 48, 44, 3, 36, 26, 50, 27, 2, 46, 4, 19, 47];

console.log(arr);
arr = mergeSort(arr);
console.log(arr);

function mergeSort(arr) {
    let len = arr.length;
    if (len < 2) return arr;

    // 将数组分为两个部分
    let middle = Math.floor(len / 2);
    let left = arr.slice(0, middle);
    let right = arr.slice(middle);
    // 注意此时传入的不是数组的索引, 而是子数组, 因为归并排序是out-place形式.
    return merge(mergeSort(left), mergeSort(right));
}

// left与right都是有序数组, 所以情况会简单很多
function merge(left, right) {
    // result占用了额外的内存空间.
    let result = [];

    while (left.length > 0 && right.length > 0) {
        if (left[0] <= right[0]) {
            result.push(left.shift());
        } else {
            result.push(right.shift());
        }
    }
    // 经过上面的while循环, left/right中只有一个数组中还有成员, 且可能不只一个
    // 如下两句将其追加到result列表中.
    while (left.length) result.push(left.shift());
    while (right.length) result.push(right.shift());

    return result;
}

```